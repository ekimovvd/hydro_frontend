import { Component, Vue } from "nuxt-property-decorator";

import { COMPONENT_NAME } from "./constants";

@Component({
  name: COMPONENT_NAME,
})
export default class BulletinsWrapperStations extends Vue {}
